isCorrectInput()
arrangeLivingRoom()
confirmDraft(List<ItemCard> pick)
AAA confirmEndTurn(List<ItemCard> pick, Int Col) --pick should be sorted
withdrawPick()
AAA leaveGame()
disconnectedPlayer(Player p)
reconnectPlayer(Player p, String gameId)

AAA+ joinGame(Game, Player p){
    if(Game.getPlayers().lenght < MaxPlayersNum){
        Game.addPlayer(Player p);
    }
    else if (Game.getPlayers().lenght = MaxPlayersNum){
        updateLivingRoom()
    }
}
AAA+ createGame(String GameId, Player creator)
AAA+ recreateGame(All the Arguments)



Model