Avvio App
Selection between server e client
-- Client StartUp
Choose Communication Type - (Socket / RMI) - Connection to Server
Choose Interface Type

-- Server StartUp
Define Communication Port

-- Client Side Events
    login()
    LivingRoom isThereAnyGameLeft(Player p)
    void createOrJoinGame()

-- Create Game
    simple method

-- Join Game
    simple method

-- Game Evolution (Turn)
    List<ItemCard> draft = pickDraft()
    void giveDraftTo(Player p)
    void updateLivingRoom()
    Int col = selectCol()
    void selectOrder()
    void orderCards()
    void placeCardsInCol(Int col, List<ItemCard> drawnCards)


    --- End Turn Fase
        void updateShelf(Player p)
        void checkCommonGoals(Player p)  -- Server
        void updateCommonGoals(Player p) -- Client
        void updateScore(Player p)
        void nextTurn()














